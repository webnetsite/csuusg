import React, { useContext } from "react";
import "./Services.css";
import Card from "../Card/Card";
import HeartEmoji from "../../img/heartemoji.png";
import Glasses from "../../img/glasses.png";
import Humble from "../../img/humble.png";
import { themeContext } from "../../Context";
import { motion } from "framer-motion";
import Student_Handbook from './Student_Handbook.pdf';

const Services = () => {
  // context
  const theme = useContext(themeContext);
  const darkMode = theme.state.darkMode;

  // transition
  const transition = {
    duration: 1,
    type: "spring",
  };

  return (
    <div className="services" id="services">
      {/* left side */}
      <div className="awesome">
        {/* dark mode */}
        <span style={{ color: darkMode ? "white" : "" }}>CSU</span>
        <span>Student Handbook</span>
        <span style={{ fontFamily: "'Roboto', sans-serif", fontSize: '16px'}}>
          A comprehensive reference, addressing academic regulations, campus services,
          <br />
          and student conduct, ensuring a successful educational journey at Caraga State University.
        </span>

        <a href={Student_Handbook} download>
          <button className="button s-button">Download PDF</button>
        </a>
        <div className="blur s-blur1" style={{ background: "#ffdfaa" }}></div>
      </div>
      {/* right */}
      <div className="cards">
        {/* first card */}
        <motion.div
          initial={{ left: "25rem" }}
          whileInView={{ left: "14rem" }}
          transition={transition}
        >
          <Card
            emoji={HeartEmoji}
            heading={"University"}
            detail={"History, Mission, Vision, and Core Values"}
          />
        </motion.div>
        {/* second card */}
        <motion.div
          initial={{ left: "-11rem", top: "12rem" }}
          whileInView={{ left: "-4rem" }}
          transition={transition}
        >
          <Card
            emoji={Glasses}
            heading={"Academic Policies"}
            detail={"Admission and Registration"}
          />
        </motion.div>
        {/* 3rd */}
        <motion.div
          initial={{ top: "19rem", left: "25rem" }}
          whileInView={{ left: "12rem" }}
          transition={transition}
        >
          <Card
            emoji={Humble}
            heading={"Student Services"}
            detail={
              "Students Rights and Responsibilities"
            }
            color="rgba(252, 166, 31, 0.45)"
          />
        </motion.div>
        <div
          className="blur s-blur2"
          style={{ background: "#ffdfaa" }}
        ></div>
      </div>
    </div>
  );
};

export default Services;
